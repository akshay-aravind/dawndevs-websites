"use client";

/**
 * A calm, elegant dark backdrop — a soft top/bottom spotlight over near-black.
 * No colour, no motion clutter; the restraint is the point.
 */
export default function Atmosphere() {
  return <div className="spotlight" aria-hidden />;
}
